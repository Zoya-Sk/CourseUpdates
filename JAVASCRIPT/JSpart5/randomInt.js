let num = Math.random();
num = num * 10;  // range from 1 to 10
num = Math.floor(num);  
// numbers will only generate from range 0 to 9
num = num + 1;  //range between 1 to 10
// console.log(num);

let shortForm = Math.floor( Math.random() * 10 ) + 1;
console.log(shortForm);